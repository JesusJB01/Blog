/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['tailwindui.com', "cdn.rareblocks.xyz", "images.unsplash.com"],

  },
}

module.exports = nextConfig
